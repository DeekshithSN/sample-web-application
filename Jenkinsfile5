@Library('javahome-demo@main')_ //master or whatever branch

pipeline{
node()
stages{
  stage('checkout scm'){
    checkout scm
  }
  stage('npm build'){
    steps{
     script{
     	sh 'react.groovy'
      }
     }  
   }	
 }                        	     	         
}
