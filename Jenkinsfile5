@Library('javahome-demo@main')_ //master or whatever branch
node()
stages{
  stage('checkout scm'){
    checkout scm
  }
  stage('build'){
   jobDs1 targets: 'jobs/**/react.groovy'  
   }	
}                        	     	         

